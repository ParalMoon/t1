git status
git branch 
git log --oneline --graph
git log --graph --decorate --all
#--들의 순서는 상관없음

git remote add t1 https://github.com/ParalMoon/t1

git add a.txt
#파일 스테이지에 올리기
git commit -m "a.txt version 1"
#스테이지에 있던 파일이 다 스냅샷
git push origin main
#원격 저장소에 올라감(gitignore 빼고)
git pull origin main

git restore --staged a.txt
git restore --staged .
#스테이지에 있는 모든 파일 내리기


git checkout main
git checkout ac20104
git checkout -

git branch br1
#브랜치 생성
git checkout -b br1
#브랜치 생성과 동시에 이동

merge
rebase
pull request





